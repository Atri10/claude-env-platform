# .claudeignore :: per-repo exclusions for indexing and agent reads.
# Placed at the repo root. The policy engine is authoritative for SECURITY
# (this file is a convenience layer for noise reduction, not a control).

# build / vendor
node_modules/
vendor/
dist/
build/
target/
.gradle/
.venv/
__pycache__/
*.pyc

# generated
**/generated/**
**/*_pb2.py
**/*.min.js
**/*.map

# large / binary
*.lock
*.bin
*.onnx
*.gguf
*.parquet
*.sqlite
*.db

# local noise
.DS_Store
.idea/
.vscode/
coverage/
.pytest_cache/
