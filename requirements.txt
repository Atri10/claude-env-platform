# claude-env runtime dependencies (installed by bootstrap.py)
# Locked versions resolved via `uv pip compile` against Python 3.13 (2026-07).
# The platform targets Python 3.13+ on macOS Apple Silicon.
# Keep in sync with PIP_DEPS_LOCKED in bootstrap.py.
pyyaml==6.0.3
lancedb==0.33.0
pyarrow==24.0.0
# install with Metal:  CMAKE_ARGS="-DLLAMA_METAL=on" pip install llama-cpp-python
llama-cpp-python==0.3.32
onnxruntime==1.27.0
transformers==5.12.1
tree-sitter==0.26.0
# successor to the abandoned tree-sitter-languages (no cp313+ wheels)
tree-sitter-language-pack==1.12.0
mcp==1.28.1
httpx==0.28.1
datasette==0.65.2
# psycopg>=3.1            # only for the optional PostgreSQL backend
# optimum[exporters]      # only to export the reranker to ONNX (one-time)
